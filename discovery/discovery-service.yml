apiVersion: v1
kind: Service
metadata:
  name: discoveryserver-service
  namespace: ecgcbac
  labels:
    name: discoveryserver-service
    app: discoveryserver
spec:
  selector:
    app: discoveryserver
  ports:
  - port: 8000
    targetPort: 8000